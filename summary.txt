- The single thread is slower at with big amounts of clients, as was expected. 
- But what was interesting was that depend on what computer is being used as the host/client since it's all self contained right now
- 2 core hard a harder time handling larger request vs an 8 core, also other things come into account.
